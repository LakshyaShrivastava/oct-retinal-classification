# GitHub repository metadata

GitHub pin cards use **Description** and **Topics** from **Settings → General** (not from files). Apply these there so your profile pins stay readable.

| Field | Value |
|--------|--------|
| **Description** | OCT retinal slice classification with PyTorch DenseNet-121, Weights & Biases experiment tracking, optional Bayesian sweeps, and Grad-CAM interpretability (notebook-driven workflow). |
| **Topics** | `pytorch`, `computer-vision`, `medical-imaging`, `densenet`, `wandb`, `grad-cam`, `oct`, `classification`, `deep-learning` |

Copy topics one at a time into the Topics field (GitHub suggests tags as you type).

**Note:** Code is licensed under `LICENSE`; **dataset** redistribution still follows the license of the OCT bundle you use (see README).
